import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glowcare.app',
  appName: 'Skincare & Haircare Analyzer',
  webDir: 'build'
};

export default config;
